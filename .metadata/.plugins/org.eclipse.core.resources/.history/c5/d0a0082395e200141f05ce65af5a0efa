import java.util.LinkedList;
import java.util.Queue;


public class BFS {
	
	int amountOfWords;
	int secondWord;
	int firstWord;
	LinkedList<Integer>[] adj;
	LinkedList<Integer> temp;
	int[] level;
	boolean[] discovered;
	
	public int run(LinkedList<Integer>[] adj, int firstWord, int secondWord, int amountOfWords) {
		discovered = new boolean[amountOfWords];
		Queue<Integer> queue = new LinkedList<Integer>();
		queue.offer(firstWord);
		 while (!queue.isEmpty()) {
			 int current = queue.poll();
			 temp = adj[current];
			 
		 }
		return 0;
	}
}
